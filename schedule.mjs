<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Staff Scheduler Pro</title>
<style>
:root{--bg:#f4f6f8;--panel:#fff;--ink:#14202b;--muted:#66727d;--line:#dce2e7;--brand:#174f7a;--ok:#177245;--warn:#a76700;--danger:#b42318;--shadow:0 8px 24px rgba(15,23,42,.08)}*{box-sizing:border-box}body{margin:0;font-family:Inter,Arial,sans-serif;background:var(--bg);color:var(--ink)}button,input,select,textarea{font:inherit}.top{position:sticky;top:0;z-index:20;background:#102f49;color:#fff;padding:12px 16px;display:flex;gap:10px;align-items:center;flex-wrap:wrap;box-shadow:var(--shadow)}.top h1{font-size:18px;margin:0 auto 0 0}.top button,.btn{border:0;border-radius:8px;padding:9px 12px;cursor:pointer;font-weight:700}.top button{background:#fff;color:#123}.status{font-size:12px;padding:7px 10px;border-radius:999px;background:#38566d}.status.live{background:#1f7a4d}.wrap{display:grid;grid-template-columns:300px 1fr;gap:14px;padding:14px}.sidebar{display:flex;flex-direction:column;gap:12px}.panel{background:var(--panel);border:1px solid var(--line);border-radius:12px;box-shadow:var(--shadow);overflow:hidden}.panel h2{font-size:14px;margin:0;padding:12px 14px;border-bottom:1px solid var(--line);display:flex;justify-content:space-between;align-items:center}.tools{display:flex;gap:6px;flex-wrap:wrap;padding:10px}.tools input,.tools select{width:100%;padding:8px;border:1px solid var(--line);border-radius:7px}.small{padding:6px 8px!important;font-size:12px}.category{border-top:1px solid var(--line)}.cat-head{padding:9px 11px;background:#eef3f7;font-size:12px;font-weight:800;display:flex;justify-content:space-between;align-items:center;gap:5px}.cat-head span{cursor:pointer}.items{padding:6px;min-height:30px}.person,.vehicle{background:#fff;border:1px solid var(--line);border-radius:8px;padding:8px;margin:5px 0;cursor:grab;display:flex;justify-content:space-between;align-items:center;gap:6px;font-size:13px}.badge{font-size:10px;padding:3px 6px;border-radius:999px;font-weight:800}.available{background:#dcfce7;color:#166534}.leave{background:#fef3c7;color:#92400e}.sick{background:#fee2e2;color:#991b1b}.assigned{background:#dbeafe;color:#1e40af}.board-wrap{min-width:0}.weekbar{background:#fff;border:1px solid var(--line);border-radius:12px;padding:10px;display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-bottom:10px}.weekbar strong{margin-right:auto}.board{display:grid;grid-template-columns:repeat(7,minmax(230px,1fr));gap:10px;overflow-x:auto;padding-bottom:10px}.day{background:#fff;border:1px solid var(--line);border-radius:12px;min-height:640px;box-shadow:var(--shadow)}.day-head{padding:11px;border-bottom:1px solid var(--line);font-weight:900;position:sticky;top:61px;background:white;z-index:4;border-radius:12px 12px 0 0}.inline{display:flex;gap:5px;padding:8px}.inline input{min-width:0;flex:1;padding:8px;border:1px solid var(--line);border-radius:7px}.job-list{padding:7px;min-height:500px}.job{border:1px solid #cfd8df;border-left:6px solid var(--job,#427aa1);border-radius:9px;margin:7px 0;background:#fff;overflow:hidden}.job.dragging{opacity:.45}.job-title{padding:9px;display:flex;gap:6px;align-items:center;font-weight:900;cursor:grab}.job-title .name{flex:1}.job-meta{padding:0 9px 8px;color:var(--muted);font-size:11px}.assign{border-top:1px dashed var(--line);padding:7px}.assign h4{font-size:10px;text-transform:uppercase;letter-spacing:.05em;margin:0 0 5px;color:var(--muted)}.chips{min-height:28px}.chip{display:inline-flex;align-items:center;gap:5px;border-radius:999px;padding:5px 7px;margin:2px;background:#edf2f6;font-size:11px}.chip button{border:0;background:transparent;cursor:pointer;color:var(--danger);padding:0}.icon{border:0;background:transparent;cursor:pointer;padding:3px}.dropover{outline:3px solid #66a3cc;outline-offset:-3px}.modal{display:none;position:fixed;inset:0;background:rgba(0,0,0,.45);z-index:50;align-items:center;justify-content:center;padding:15px}.modal.show{display:flex}.dialog{background:#fff;width:min(650px,100%);max-height:90vh;overflow:auto;border-radius:14px;padding:16px}.dialog h3{margin-top:0}.grid2{display:grid;grid-template-columns:1fr 1fr;gap:10px}.field{margin:9px 0}.field label{display:block;font-size:12px;font-weight:800;margin-bottom:4px}.field input,.field select,.field textarea{width:100%;padding:9px;border:1px solid var(--line);border-radius:7px}.actions{display:flex;justify-content:flex-end;gap:8px;margin-top:14px}.danger{background:#fee2e2;color:#991b1b}.primary{background:#174f7a;color:white}.ghost{background:#eef2f5}.empty{font-size:11px;color:#89939b;padding:7px;text-align:center}.toast{position:fixed;right:16px;bottom:16px;background:#132;color:#fff;padding:10px 14px;border-radius:8px;display:none;z-index:70}.toast.show{display:block}.print-only{display:none}@media(max-width:900px){.wrap{grid-template-columns:1fr}.sidebar{order:2}.board{grid-template-columns:repeat(7,260px)}.top{position:static}.day-head{top:0}}@media print{.top,.sidebar,.weekbar,.inline,.icon{display:none!important}.wrap{display:block;padding:0}.board{display:grid;grid-template-columns:repeat(2,1fr);overflow:visible}.day{min-height:0;break-inside:avoid}.job-list{min-height:0}.print-only{display:block}}
</style></head><body>
<header class="top"><h1>Staff Scheduler Pro</h1><button onclick="undo()">Undo</button><button onclick="redo()">Redo</button><button onclick="copyPreviousDay()">Copy Previous Day</button><button onclick="copyPreviousWeek()">Copy Previous Week</button><button onclick="window.print()">Print</button><button onclick="backup()">Backup</button><button onclick="restoreClick()">Restore</button><span id="status" class="status">Connecting…</span></header>
<div class="wrap"><aside class="sidebar">
<section class="panel"><h2>Staff <button class="btn small primary" onclick="openStaff()">Manage</button></h2><div class="tools"><input id="staffSearch" placeholder="Search staff" oninput="renderSidebar()"></div><div id="staffPanel"></div></section>
<section class="panel"><h2>Vehicles <button class="btn small primary" onclick="openVehicle()">Manage</button></h2><div class="tools"><input id="vehicleSearch" placeholder="Search vehicles" oninput="renderSidebar()"></div><div id="vehiclePanel"></div></section>
</aside><main class="board-wrap"><div class="weekbar"><button class="btn ghost" onclick="moveWeek(-1)">←</button><button class="btn ghost" onclick="goToday()">Today</button><button class="btn ghost" onclick="moveWeek(1)">→</button><strong id="weekLabel"></strong><select id="statusFilter" onchange="renderAll()"><option value="">All staff statuses</option><option>Available</option><option>On Leave</option><option>Sick</option></select></div><div id="board" class="board"></div></main></div>
<div id="modal" class="modal"><div class="dialog" id="dialog"></div></div><div id="toast" class="toast"></div><input type="file" id="restoreFile" hidden accept="application/json" onchange="restore(event)">
<script>
const DAYS=['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'];
const COLORS=['#427aa1','#8e5ea2','#2f855a','#c05621','#b83280','#4a5568','#b7791f'];
const initialStaff=['BEN GARDNER','JIALANG GU','ANDY TABAR','MARK BANICO','GLENN OULD','FRANCIS TRONG LUAT HO','SIMON LANG','Borko Mitrovic','Vedran Zoraja','Martin Quan','RYAN HAYWOOD','JUAN GIRALDOPA','WINSTON PHAM','JUSTIN PHAM','MARIO MOTTA','OZGUR OZTURK','WENCESLAO PATRITIO','KILIN HATTORI','JACK NEWMAN','CAOLAN ROONEY','BRANDON LE','COLIN HUGHES','BEN DEWSE','DAEWON HONG','LUKE RAYNOR','BRAINER VENEGAS','CRISTIAN CHAVARRIAGA','KINGROY SUSON'];
const uid=()=>crypto.randomUUID?crypto.randomUUID():Date.now().toString(36)+Math.random().toString(36).slice(2);
const defaultState=()=>({version:1,staffCategories:[{id:'ft',name:'FULL TIME - WORKSTATION'},{id:'casual',name:'CASUAL'}],staff:initialStaff.map((n,i)=>({id:uid(),name:n,categoryId:i<7?'ft':'casual',status:'Available'})),vehicleCategories:[{id:'trucks',name:'TRUCKS'},{id:'vans',name:'VANS / UTES'}],vehicles:[],weeks:{},updatedAt:null});
let state=defaultState(),weekStart=mondayOf(new Date()),history=[],future=[],saveTimer=null,lastServerUpdate=null,drag=null;
function mondayOf(d){d=new Date(d);d.setHours(0,0,0,0);const x=(d.getDay()+6)%7;d.setDate(d.getDate()-x);return d}function keyDate(d){return d.toISOString().slice(0,10)}function esc(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function ensureWeek(k=keyDate(weekStart)){if(!state.weeks[k])state.weeks[k]={jobs:[]};return state.weeks[k]}
function snapshot(){history.push(JSON.stringify(state));if(history.length>40)history.shift();future=[]}function mutate(fn){snapshot();fn();persist();renderAll()}function undo(){if(!history.length)return;future.push(JSON.stringify(state));state=JSON.parse(history.pop());persist();renderAll()}function redo(){if(!future.length)return;history.push(JSON.stringify(state));state=JSON.parse(future.pop());persist();renderAll()}
function persist(){localStorage.setItem('staffSchedulerPro',JSON.stringify(state));clearTimeout(saveTimer);saveTimer=setTimeout(saveOnline,350)}
async function saveOnline(){try{const r=await fetch('/.netlify/functions/schedule',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify(state)});if(!r.ok)throw Error('save failed');const j=await r.json();lastServerUpdate=j.data.updatedAt;setStatus(true)}catch(e){setStatus(false)}}
async function loadOnline(){const local=localStorage.getItem('staffSchedulerPro');if(local)try{state=JSON.parse(local)}catch{};try{const r=await fetch('/.netlify/functions/schedule',{cache:'no-store'});if(!r.ok)throw Error();const j=await r.json();if(j.data){state=j.data;localStorage.setItem('staffSchedulerPro',JSON.stringify(state));lastServerUpdate=state.updatedAt}setStatus(true)}catch{setStatus(false)}renderAll()}
async function poll(){try{const r=await fetch('/.netlify/functions/schedule',{cache:'no-store'});if(!r.ok)return;const j=await r.json();if(j.data?.updatedAt&&j.data.updatedAt!==lastServerUpdate){state=j.data;lastServerUpdate=j.data.updatedAt;localStorage.setItem('staffSchedulerPro',JSON.stringify(state));renderAll();toast('Schedule updated by another user')}setStatus(true)}catch{setStatus(false)}}
function setStatus(ok){const e=document.getElementById('status');e.className='status '+(ok?'live':'');e.textContent=ok?'Live · saved online':'Offline · saved locally'}
function renderAll(){renderSidebar();renderBoard();document.getElementById('weekLabel').textContent='Week of '+weekStart.toLocaleDateString('en-AU',{day:'numeric',month:'long',year:'numeric'})}
function renderSidebar(){const sq=(document.getElementById('staffSearch')?.value||'').toLowerCase(),vq=(document.getElementById('vehicleSearch')?.value||'').toLowerCase(),filter=document.getElementById('statusFilter')?.value||'';document.getElementById('staffPanel').innerHTML=state.staffCategories.map(c=>{const a=state.staff.filter(x=>x.categoryId===c.id&&x.name.toLowerCase().includes(sq)&&(!filter||x.status===filter));return `<div class="category"><div class="cat-head"><span>${esc(c.name)}</span><small>${a.length}</small></div><div class="items" data-cat="${c.id}" ondragover="event.preventDefault()" ondrop="dropStaffCategory(event,'${c.id}')">${a.map(personHtml).join('')||'<div class="empty">No staff</div>'}</div></div>`}).join('');document.getElementById('vehiclePanel').innerHTML=state.vehicleCategories.map(c=>{const a=state.vehicles.filter(x=>x.categoryId===c.id&&x.name.toLowerCase().includes(vq));return `<div class="category"><div class="cat-head"><span>${esc(c.name)}</span><small>${a.length}</small></div><div class="items" ondragover="event.preventDefault()" ondrop="dropVehicleCategory(event,'${c.id}')">${a.map(vehicleHtml).join('')||'<div class="empty">No vehicles</div>'}</div></div>`}).join('')}
function personHtml(p){const cls=p.status==='Available'?'available':p.status==='On Leave'?'leave':p.status==='Sick'?'sick':'assigned';return `<div class="person" draggable="true" ondragstart="dragStart(event,'staff','${p.id}')"><span>${esc(p.name)}</span><span class="badge ${cls}">${esc(p.status)}</span></div>`}function vehicleHtml(v){return `<div class="vehicle" draggable="true" ondragstart="dragStart(event,'vehicle','${v.id}')"><span>🚚 ${esc(v.name)}</span></div>`}
function renderBoard(){const w=ensureWeek();document.getElementById('board').innerHTML=DAYS.map((d,di)=>{const date=new Date(weekStart);date.setDate(date.getDate()+di);const jobs=w.jobs.filter(j=>j.day===di).sort((a,b)=>(a.order||0)-(b.order||0));return `<section class="day" ondragover="event.preventDefault()" ondrop="dropDay(event,${di})"><div class="day-head">${d}<br><small>${date.toLocaleDateString('en-AU',{day:'numeric',month:'short'})}</small></div><div class="inline"><input id="quick-${di}" placeholder="Add job to ${d}" onkeydown="if(event.key==='Enter')quickAdd(${di})"><button class="btn primary" onclick="quickAdd(${di})">+</button></div><div class="job-list">${jobs.map(jobHtml).join('')||'<div class="empty">Drop or add a job here</div>'}</div></section>`}).join('')}
function jobHtml(j){const staff=(j.staffIds||[]).map(id=>state.staff.find(x=>x.id===id)).filter(Boolean),vehicles=(j.vehicleIds||[]).map(id=>state.vehicles.find(x=>x.id===id)).filter(Boolean);return `<article class="job" draggable="true" style="--job:${j.color||COLORS[0]}" ondragstart="dragStart(event,'job','${j.id}')" ondragover="event.preventDefault();this.classList.add('dropover')" ondragleave="this.classList.remove('dropover')" ondrop="dropJob(event,'${j.id}')"><div class="job-title"><span class="name">${esc(j.name)}</span><button class="icon" onclick="event.stopPropagation();editJob('${j.id}')">✏️</button><button class="icon" onclick="event.stopPropagation();deleteJob('${j.id}')">🗑️</button></div>${j.address||j.notes?`<div class="job-meta">${esc(j.address||'')}${j.address&&j.notes?' · ':''}${esc(j.notes||'')}</div>`:''}<div class="assign"><h4>Staff</h4><div class="chips">${staff.map(p=>`<span class="chip">${esc(p.name)} <button onclick="removeAssign('${j.id}','staff','${p.id}')">×</button></span>`).join('')||'<span class="empty">Drop staff here</span>'}</div></div><div class="assign"><h4>Vehicles</h4><div class="chips">${vehicles.map(v=>`<span class="chip">🚚 ${esc(v.name)} <button onclick="removeAssign('${j.id}','vehicle','${v.id}')">×</button></span>`).join('')||'<span class="empty">Drop vehicles here</span>'}</div></div></article>`}
function dragStart(e,type,id){drag={type,id};e.dataTransfer.setData('text/plain',JSON.stringify(drag));e.dataTransfer.effectAllowed=type==='job'?'move':'copy'}function getDrag(e){try{return JSON.parse(e.dataTransfer.getData('text/plain'))}catch{return drag}}
function dropDay(e,day){e.preventDefault();const d=getDrag(e);if(d?.type==='job')mutate(()=>{const j=ensureWeek().jobs.find(x=>x.id===d.id);if(j)j.day=day})}
function dropJob(e,jobId){e.preventDefault();e.stopPropagation();e.currentTarget.classList.remove('dropover');const d=getDrag(e);if(!d)return;if(d.type==='staff')mutate(()=>{const j=ensureWeek().jobs.find(x=>x.id===jobId);j.staffIds=j.staffIds||[];if(!j.staffIds.includes(d.id))j.staffIds.push(d.id)});if(d.type==='vehicle')mutate(()=>{const j=ensureWeek().jobs.find(x=>x.id===jobId);j.vehicleIds=j.vehicleIds||[];if(!j.vehicleIds.includes(d.id))j.vehicleIds.push(d.id)});if(d.type==='job'&&d.id!==jobId)mutate(()=>{const jobs=ensureWeek().jobs,a=jobs.find(x=>x.id===d.id),b=jobs.find(x=>x.id===jobId);if(a&&b){a.day=b.day;const o=a.order;a.order=b.order;b.order=o}})}
function dropStaffCategory(e,cid){const d=getDrag(e);if(d?.type==='staff')mutate(()=>{const p=state.staff.find(x=>x.id===d.id);if(p)p.categoryId=cid})}function dropVehicleCategory(e,cid){const d=getDrag(e);if(d?.type==='vehicle')mutate(()=>{const v=state.vehicles.find(x=>x.id===d.id);if(v)v.categoryId=cid})}
function quickAdd(day){const i=document.getElementById('quick-'+day),name=i.value.trim();if(!name)return;mutate(()=>ensureWeek().jobs.push({id:uid(),name,day,order:Date.now(),address:'',notes:'',color:COLORS[ensureWeek().jobs.length%COLORS.length],staffIds:[],vehicleIds:[]}));i.value=''}
function findJob(id){return ensureWeek().jobs.find(x=>x.id===id)}function editJob(id){const j=findJob(id);show(`<h3>Edit job</h3><div class="field"><label>Job name</label><input id="jn" value="${esc(j.name)}"></div><div class="field"><label>Address</label><input id="ja" value="${esc(j.address||'')}"></div><div class="field"><label>Notes</label><textarea id="jt">${esc(j.notes||'')}</textarea></div><div class="field"><label>Colour</label><input id="jc" type="color" value="${j.color||COLORS[0]}"></div><div class="actions"><button class="btn ghost" onclick="hide()">Cancel</button><button class="btn primary" onclick="saveJob('${id}')">Save</button></div>`)}function saveJob(id){mutate(()=>{const j=findJob(id);j.name=jn.value.trim()||j.name;j.address=ja.value.trim();j.notes=jt.value.trim();j.color=jc.value});hide()}function deleteJob(id){if(confirm('Delete this job?'))mutate(()=>{const w=ensureWeek();w.jobs=w.jobs.filter(x=>x.id!==id)})}function removeAssign(jobId,type,id){mutate(()=>{const j=findJob(jobId),k=type==='staff'?'staffIds':'vehicleIds';j[k]=(j[k]||[]).filter(x=>x!==id)})}
function openStaff(){show(`<h3>Manage staff and categories</h3><div class="grid2"><div><h4>Categories</h4>${state.staffCategories.map(c=>`<div class="person"><span>${esc(c.name)}</span><span><button class="icon" onclick="renameStaffCat('${c.id}')">✏️</button><button class="icon" onclick="deleteStaffCat('${c.id}')">🗑️</button></span></div>`).join('')}<button class="btn ghost" onclick="addStaffCat()">+ Category</button></div><div><h4>Staff</h4>${state.staff.map(p=>`<div class="person"><span>${esc(p.name)}<br><small>${esc(p.status)}</small></span><span><button class="icon" onclick="editStaff('${p.id}')">✏️</button><button class="icon" onclick="deleteStaff('${p.id}')">🗑️</button></span></div>`).join('')}<button class="btn primary" onclick="addStaff()">+ Staff</button></div></div><div class="actions"><button class="btn ghost" onclick="hide()">Close</button></div>`)}
function addStaffCat(){const n=prompt('Category name');if(n)mutate(()=>state.staffCategories.push({id:uid(),name:n.trim()}));openStaff()}function renameStaffCat(id){const c=state.staffCategories.find(x=>x.id===id),n=prompt('Rename category',c.name);if(n)mutate(()=>c.name=n.trim());openStaff()}function deleteStaffCat(id){if(state.staffCategories.length<2)return alert('Keep at least one category');if(confirm('Delete category? Staff will move to the first remaining category.'))mutate(()=>{state.staffCategories=state.staffCategories.filter(x=>x.id!==id);state.staff.forEach(p=>{if(p.categoryId===id)p.categoryId=state.staffCategories[0].id})});openStaff()}
function addStaff(){staffForm()}function editStaff(id){staffForm(state.staff.find(x=>x.id===id))}function staffForm(p){show(`<h3>${p?'Edit':'Add'} staff</h3><div class="field"><label>Name</label><input id="pn" value="${esc(p?.name||'')}"></div><div class="field"><label>Category</label><select id="pc">${state.staffCategories.map(c=>`<option value="${c.id}" ${p?.categoryId===c.id?'selected':''}>${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label>Status</label><select id="ps">${['Available','On Leave','Sick'].map(s=>`<option ${p?.status===s?'selected':''}>${s}</option>`).join('')}</select></div><div class="actions"><button class="btn ghost" onclick="openStaff()">Back</button><button class="btn primary" onclick="saveStaff('${p?.id||''}')">Save</button></div>`)}function saveStaff(id){const n=pn.value.trim();if(!n)return;if(id)mutate(()=>Object.assign(state.staff.find(x=>x.id===id),{name:n,categoryId:pc.value,status:ps.value}));else mutate(()=>state.staff.push({id:uid(),name:n,categoryId:pc.value,status:ps.value}));openStaff()}function deleteStaff(id){if(confirm('Delete this staff member and remove their job assignments?'))mutate(()=>{state.staff=state.staff.filter(x=>x.id!==id);Object.values(state.weeks).forEach(w=>w.jobs.forEach(j=>j.staffIds=(j.staffIds||[]).filter(x=>x!==id)))});openStaff()}
function openVehicle(){show(`<h3>Manage vehicles and categories</h3><div class="grid2"><div><h4>Categories</h4>${state.vehicleCategories.map(c=>`<div class="vehicle"><span>${esc(c.name)}</span><span><button class="icon" onclick="renameVehicleCat('${c.id}')">✏️</button><button class="icon" onclick="deleteVehicleCat('${c.id}')">🗑️</button></span></div>`).join('')}<button class="btn ghost" onclick="addVehicleCat()">+ Category</button></div><div><h4>Vehicles</h4>${state.vehicles.map(v=>`<div class="vehicle"><span>${esc(v.name)}</span><span><button class="icon" onclick="editVehicle('${v.id}')">✏️</button><button class="icon" onclick="deleteVehicle('${v.id}')">🗑️</button></span></div>`).join('')}<button class="btn primary" onclick="addVehicle()">+ Vehicle</button></div></div><div class="actions"><button class="btn ghost" onclick="hide()">Close</button></div>`)}
function addVehicleCat(){const n=prompt('Vehicle category name');if(n)mutate(()=>state.vehicleCategories.push({id:uid(),name:n.trim()}));openVehicle()}function renameVehicleCat(id){const c=state.vehicleCategories.find(x=>x.id===id),n=prompt('Rename category',c.name);if(n)mutate(()=>c.name=n.trim());openVehicle()}function deleteVehicleCat(id){if(state.vehicleCategories.length<2)return alert('Keep at least one category');if(confirm('Delete category? Vehicles will move to the first remaining category.'))mutate(()=>{state.vehicleCategories=state.vehicleCategories.filter(x=>x.id!==id);state.vehicles.forEach(v=>{if(v.categoryId===id)v.categoryId=state.vehicleCategories[0].id})});openVehicle()}
function addVehicle(){vehicleForm()}function editVehicle(id){vehicleForm(state.vehicles.find(x=>x.id===id))}function vehicleForm(v){show(`<h3>${v?'Edit':'Add'} vehicle</h3><div class="field"><label>Vehicle name / registration</label><input id="vn" value="${esc(v?.name||'')}"></div><div class="field"><label>Category</label><select id="vc">${state.vehicleCategories.map(c=>`<option value="${c.id}" ${v?.categoryId===c.id?'selected':''}>${esc(c.name)}</option>`).join('')}</select></div><div class="actions"><button class="btn ghost" onclick="openVehicle()">Back</button><button class="btn primary" onclick="saveVehicle('${v?.id||''}')">Save</button></div>`)}function saveVehicle(id){const n=vn.value.trim();if(!n)return;if(id)mutate(()=>Object.assign(state.vehicles.find(x=>x.id===id),{name:n,categoryId:vc.value}));else mutate(()=>state.vehicles.push({id:uid(),name:n,categoryId:vc.value}));openVehicle()}function deleteVehicle(id){if(confirm('Delete this vehicle and remove its job assignments?'))mutate(()=>{state.vehicles=state.vehicles.filter(x=>x.id!==id);Object.values(state.weeks).forEach(w=>w.jobs.forEach(j=>j.vehicleIds=(j.vehicleIds||[]).filter(x=>x!==id)))});openVehicle()}
function moveWeek(n){weekStart.setDate(weekStart.getDate()+7*n);weekStart=new Date(weekStart);renderAll()}function goToday(){weekStart=mondayOf(new Date());renderAll()}function cloneJobs(jobs,dayShift=0){return jobs.map(j=>({...j,id:uid(),day:j.day+dayShift,staffIds:[...(j.staffIds||[])],vehicleIds:[...(j.vehicleIds||[])]}))}function copyPreviousWeek(){const prev=new Date(weekStart);prev.setDate(prev.getDate()-7);const src=state.weeks[keyDate(prev)]?.jobs||[];if(!src.length)return alert('Previous week has no jobs');if(confirm('Replace this week with a copy of the previous week?'))mutate(()=>state.weeks[keyDate(weekStart)]={jobs:cloneJobs(src)})}function copyPreviousDay(){const day=Number(prompt('Copy which day into the following day? Enter 1 for Monday through 6 for Saturday.','1'))-1;if(day<0||day>5)return;const src=ensureWeek().jobs.filter(j=>j.day===day);if(!src.length)return alert('That day has no jobs');mutate(()=>ensureWeek().jobs.push(...cloneJobs(src,1)))}
function backup(){const b=new Blob([JSON.stringify(state,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='staff-scheduler-backup-'+new Date().toISOString().slice(0,10)+'.json';a.click();URL.revokeObjectURL(a.href)}function restoreClick(){restoreFile.click()}function restore(e){const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=()=>{try{const x=JSON.parse(r.result);snapshot();state=x;persist();renderAll();toast('Backup restored')}catch{alert('Invalid backup file')}};r.readAsText(f)}
function show(html){dialog.innerHTML=html;modal.classList.add('show')}function hide(){modal.classList.remove('show')}function toast(t){const e=document.getElementById('toast');e.textContent=t;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),2200)}modal.addEventListener('click',e=>{if(e.target===modal)hide()});
loadOnline();setInterval(poll,4000);
</script></body></html>
